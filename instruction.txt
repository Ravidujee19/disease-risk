# Windows (PowerShell)
py -3.11 -m venv .venv
.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt

# mac
python3.11 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt



# run the .py before preprocess.
# Everything (PNGs + CSV + HTML):
python src/eda_only.py --mode all

# Only classic outputs (PNGs + CSV), no HTML:
python src/eda_only.py --mode plots

# Only build HTML (reuses existing PNGs + CSV):
python src/eda_only.py --mode html

# if want to clean the data before run this
Remove-Item -Force -ErrorAction SilentlyContinue reports\before_preprocessed\dataset_profile.csv
Remove-Item -Force -ErrorAction SilentlyContinue reports\before_preprocessed\EDA_Report.html
Remove-Item -Recurse -Force -ErrorAction SilentlyContinue reports\figures\before_preprocessed\*


python models/Feature_Engineering_And_Modeling.py



# run after training
.venv\Scripts\Activate.ps1
streamlit run app/streamlit_app.py --server.port 8501
streamlit run app/demo_app.py --server.port 8502 # for the dev

